Implement HASH_TABLE
Simplify implementation of "remove" in SORTED_SET?
Add "remove" to set iterator?
Test sets and tables

Known bugs:

- Search doesn't work properly if model objects put inside data structures (wrong meaning of the postcondition)